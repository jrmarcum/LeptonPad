target/
version_history/
dist/
.env
.env.*
!.env.example
!.env.api.example

# DNS zone exports (e.g. cmem/clerk-leptonpad.com.zone) — registrar records and host names,
# private infrastructure detail. Keep them local; never commit one.
*.zone
