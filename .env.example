# LeptonPad — Supabase configuration
# Copy to .env and fill in your values, then rebuild.
# These values are PUBLIC (Supabase anon key is safe to expose — RLS protects data).

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key
